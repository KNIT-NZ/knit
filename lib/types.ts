export type ManifestItem = {
  slug: string;
  title: string;
  part?: string;
  order: number;
};

export type SectionData = {
  slug: string;
  title: string;
  part?: string;
  order: number;
  content: string;
};